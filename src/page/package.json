{
  "name": "sfk-schedule-landing-page",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "clean": "rm -rf dist",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "bash deploy.sh $BUCKET_NAME $DISTRIBUTION_ID"
  },
  "dependencies": {
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.6.2",
    "typescript": "^5.9.3",
    "vite": "^4.5.14",
    "vue-tsc": "^3.2.7"
  }
}
